{
  "name": "matharena",
  "version": "0.1.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "db:reset": "rm -rf .wrangler/state/v3/d1 && echo y | wrangler d1 migrations apply matharena --local"
  },
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.124.0"
  },
  "dependencies": {
    "hono": "^4.13.3"
  }
}
